services:
  aldus:
    build: .
    image: ghcr.io/mahcks/aldus
    ports:
      - "8080:8080"
    volumes:
      - aldus-data:/data
      - ./test-fixtures/alice/media:/fixtures/alice:ro
    environment:
      ALDUS_FIXTURE_DIR: /fixtures/alice

volumes:
  aldus-data:
